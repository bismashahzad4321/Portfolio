module.exports = {
  theme: {
    extend: {
      colors: {
        black: 'var(--black)',
        lBlack: 'var(--lBlack)',
        green: 'var(--green)',
        gray: 'var(--gray)',
        white: 'var(--white)',
      },
      fontFamily: {
        sans: ['Space Grotesk', 'sans-serif'],
      },
      fontSize: {
        // xlHeading: '88px',
        // lHeading: '48px',
        // mHeading: '24px',
      },
    },
  },
};
